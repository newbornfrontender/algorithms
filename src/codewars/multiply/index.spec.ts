import { multiply } from '.';

describe('codewars | multiply (8 kyu)', () => {
  test('should return 4', () => expect(multiply(2, 2)).toBe(4));
  test('should return 64', () => expect(multiply(8, 8)).toBe(64));
});
